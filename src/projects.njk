---
layout: base.njk
title: Projects | The Arcades' Studio
---

<div class="max-w-5xl mx-auto">
  <div class="text-center mb-16">
    <h1 class="gradient-text mb-8">
      Projects
    </h1>
    
    <p class="text-xl bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm p-6 rounded-xl shadow-soft dark:shadow-dark-soft inline-block max-w-2xl dark:text-gray-300 border-2 border-transparent hover:border-arcades-blue dark:hover:border-blue-500 transition-all duration-300">
      Check out my latest projects and creations. From web apps to creative coding experiments!
    </p>
  </div>
  
  <div class="space-y-24">
    {%- for project in collections.project | reverse -%}
      <article id="{{ project.data.title | slug }}" class="scroll-mt-24">
        <!-- Project Header -->
        <div class="bg-gradient-to-br from-arcades-{{ loop.index % 5 + 1 | string }}/10 to-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-100 dark:from-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-900/20 dark:to-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-900/10 p-8 rounded-xl shadow-medium dark:shadow-dark-medium border-2 border-transparent hover:border-arcades-{{ loop.index % 5 + 1 | string }} dark:hover:border-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-500 transition-all duration-300 mb-8">
          <div class="flex flex-col md:flex-row md:items-center gap-6">
            {% if project.data.icon %}
              <div class="w-16 h-16 bg-arcades-{{ loop.index % 5 + 1 | string }} rounded-lg flex items-center justify-center text-white">
                <img src="{{ project.data.icon }}" alt="{{ project.data.title }} icon" class="w-8 h-8">
              </div>
            {% else %}
              <div class="w-16 h-16 bg-arcades-{{ loop.index % 5 + 1 | string }} rounded-lg flex items-center justify-center text-white">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                </svg>
              </div>
            {% endif %}
            
            <div class="flex-grow">
              <h2 class="text-3xl md:text-4xl font-display font-bold text-arcades-{{ loop.index % 5 + 1 | string }} dark:text-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-300">
                {{ project.data.title }}
              </h2>
              
              {% if project.data.subtitle %}
                <p class="text-lg text-gray-700 dark:text-gray-300 mt-2">{{ project.data.subtitle }}</p>
              {% endif %}
            </div>
            
            <div class="flex flex-wrap gap-2">
              {% if project.data.liveUrl %}
                <a href="{{ project.data.liveUrl }}" target="_blank" rel="noopener noreferrer" class="btn btn-primary inline-flex items-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                  </svg>
                  Live Demo
                </a>
              {% endif %}
              
              {% if project.data.repoUrl %}
                <a href="{{ project.data.repoUrl }}" target="_blank" rel="noopener noreferrer" class="btn btn-secondary inline-flex items-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                  Source Code
                </a>
              {% endif %}
            </div>
          </div>
        </div>
        
        <!-- Project Images -->
        {% if project.data.images and project.data.images.length > 0 %}
          <div class="mb-8">
            <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-medium dark:shadow-dark-medium overflow-hidden">
              {% if project.data.images.length == 1 %}
                <img src="{{ project.data.images[0] }}" alt="{{ project.data.title }}" class="w-full h-auto rounded-lg">
              {% else %}
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  {% for image in project.data.images %}
                    <div class="overflow-hidden rounded-lg">
                      <img src="{{ image }}" alt="{{ project.data.title }} screenshot" class="w-full h-auto hover:scale-105 transition-transform duration-500">
                    </div>
                  {% endfor %}
                </div>
              {% endif %}
            </div>
          </div>
        {% endif %}
        
        <!-- Project Overview -->
        <div class="mb-8">
          <div class="bg-white/90 dark:bg-gray-800/90 p-8 rounded-xl shadow-medium dark:shadow-dark-medium">
            <h3 class="text-2xl font-display font-bold mb-6 text-arcades-{{ loop.index % 5 + 1 | string }} dark:text-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-300">
              Overview
            </h3>
            
            <div class="prose prose-lg dark:prose-invert max-w-none">
              {{ project.content | safe }}
            </div>
            
            {% if project.data.technologies and project.data.technologies.length > 0 %}
              <div class="mt-8">
                <h4 class="text-xl font-display font-bold mb-4 text-gray-700 dark:text-gray-300">Technologies Used</h4>
                <div class="flex flex-wrap gap-2">
                  {% for tech in project.data.technologies %}
                    <span class="bg-arcades-{{ loop.index % 5 + 1 | string }}/10 text-arcades-{{ loop.index % 5 + 1 | string }} dark:bg-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-900/20 dark:text-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-300 px-3 py-1 rounded-lg text-sm font-medium">
                      {{ tech }}
                    </span>
                  {% endfor %}
                </div>
              </div>
            {% endif %}
          </div>
        </div>
        
        <!-- Related Blog Posts -->
        {% if project.data.tags %}
          {% set relatedPosts = collections.post | relatedByTags(project.data.tags) | reverse %}
          
          {% if relatedPosts.length > 0 %}
            <div class="mb-8">
              <h3 class="text-2xl font-display font-bold mb-6 text-arcades-{{ loop.index % 5 + 1 | string }} dark:text-{{ ['pink', 'green', 'blue', 'purple', 'orange'][loop.index % 5] }}-300">
                Related Blog Posts
              </h3>
              
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                {% for post in relatedPosts | limit(4) %}
                  <a href="{{ post.url }}" class="card hover:-translate-y-2 transition duration-300 flex flex-col h-full group">
                    {% if post.data.image %}
                      <div class="h-48 bg-gray-100 dark:bg-gray-700 overflow-hidden">
                        <img src="{{ post.data.image }}" alt="{{ post.data.title }}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                      </div>
                    {% endif %}
                    
                    <div class="p-6 flex-grow flex flex-col">
                      <h4 class="text-xl font-display font-bold mb-3 text-arcades-blue dark:text-blue-300 group-hover:text-arcades-purple dark:group-hover:text-purple-300 transition-colors">
                        {{ post.data.title }}
                      </h4>
                      
                      <time datetime="{{ post.date | dateIso }}" class="bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-lg text-sm dark:text-gray-300 inline-block mb-3">
                        {{ post.date | dateReadable }}
                      </time>
                      
                      {% if post.data.description %}
                        <p class="text-gray-700 dark:text-gray-300 mb-4 flex-grow">{{ post.data.description }}</p>
                      {% endif %}
                      
                      <span class="text-arcades-purple dark:text-purple-300 font-medium inline-flex items-center group-hover:translate-x-2 transition-transform">
                        Read more
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                        </svg>
                      </span>
                    </div>
                  </a>
                {% endfor %}
              </div>
            </div>
          {% endif %}
        {% endif %}
      </article>
    {%- endfor -%}
    
    {% if collections.project.length === 0 %}
      <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-medium dark:shadow-dark-medium text-center">
        <div class="flex flex-col items-center">
          <div class="w-32 h-32 mx-auto mb-6">
            <img src="/img/avatar/demon-character.svg" alt="Demon Character Avatar" class="w-full h-full animate-float">
          </div>
          <p class="text-2xl font-display text-gray-500 dark:text-gray-400">No projects yet. Check back soon!</p>
          <p class="text-gray-500 dark:text-gray-400 mt-4">I'm working on some exciting projects that will be showcased here.</p>
        </div>
      </div>
    {% endif %}
  </div>
</div> 